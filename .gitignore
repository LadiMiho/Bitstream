## .NET
bin/
obj/
*.user
*.suo
.vs/
artifacts/
publish/
TestResults/

## Tailwind — standalone CLI binary (downloaded on demand, not committed) and generated CSS
tools/tailwindcss/
src/Bitstream.Api/wwwroot/css/app.css

## Per-machine MSBuild overrides (e.g. NuGetAudit=false behind a proxy that can't reach nuget.org)
Directory.Build.local.props

## Local secrets — never commit (TR-SEC-28)
appsettings.Local.json
appsettings.*.Local.json
*.pfx
*.snk
.env

@{
    ViewData["Title"] = "ISP Administration";
    var canCreate = (bool)(ViewBag.CanCreate ?? false);
    var canLock = (bool)(ViewBag.CanLock ?? false);
}

<div class="grid gap-6"
     data-role="isp-admin-page"
     data-can-lock="@canLock.ToString().ToLowerInvariant()">
    <div class="flex items-center justify-between">
        <div>
            <a href="/AccessManagement" class="text-sm text-brand-600 underline">&larr; Access Management</a>
            <h1 class="mt-2 text-xl font-semibold">ISP Administration</h1>
        </div>

        @if (canCreate)
        {
            <button type="button" id="isp-add-button" class="btn-primary">Add ISP</button>
        }
    </div>

    <section class="card" aria-labelledby="isp-search-heading">
        <h2 id="isp-search-heading" class="sr-only">ISPs</h2>

        <div class="flex flex-wrap items-center gap-3">
            <form id="isp-search-form" class="search-input-group flex-1 min-w-[16rem] max-w-sm" novalidate>
                <label class="sr-only" for="isp-search-query">Search by name or NIPT</label>
                <span class="search-input-icon" aria-hidden="true">
                    <svg viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.7" class="h-4 w-4"><circle cx="9" cy="9" r="6" /><path d="m17 17-3.5-3.5" stroke-linecap="round" /></svg>
                </span>
                <input class="field-input mt-0" id="isp-search-query" name="search" type="search" placeholder="Search by name or NIPT" />
            </form>

            <div class="relative">
                <button type="button" id="isp-filters-button" class="btn-secondary" aria-expanded="false" aria-controls="isp-filters-panel">
                    <svg viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.7" class="h-4 w-4"><path d="M3 4h14M6 10h8M9 16h2" stroke-linecap="round" /></svg>
                    Filters
                    <span id="isp-filters-count" class="badge bg-brand-50 text-brand-700" hidden>0</span>
                </button>

                <div id="isp-filters-panel" class="popover-panel" hidden>
                    <form id="isp-filters-form" novalidate>
                        <div class="grid grid-cols-1 gap-3">
                            <div>
                                <label class="field-label" for="isp-filter-status">Status</label>
                                <select class="field-input" id="isp-filter-status" name="status">
                                    <option value="">All</option>
                                    <option value="Active">Active</option>
                                    <option value="Locked">Locked</option>
                                </select>
                            </div>
                        </div>
                        <div class="mt-4 flex justify-end gap-2">
                            <button type="button" id="isp-filters-reset" class="btn-secondary">Reset</button>
                            <button type="submit" class="btn-primary">Apply</button>
                        </div>
                    </form>
                </div>
            </div>

            <div id="isp-filter-chips" class="flex flex-wrap items-center gap-2"></div>
        </div>

        <p id="isp-search-error" class="field-error mt-2" role="alert" hidden></p>

        <div class="mt-4 overflow-x-auto">
            <table class="w-full text-left text-sm">
                <thead>
                    <tr>
                        <th class="table-head">Name</th>
                        <th class="table-head">Contact</th>
                        <th class="table-head">Status</th>
                        <th class="table-head"><span class="sr-only">Actions</span></th>
                    </tr>
                </thead>
                <tbody id="isp-search-results"></tbody>
            </table>
            <p id="isp-search-empty" class="mt-3 text-sm text-ink-muted" hidden>No ISPs match.</p>
        </div>

        <div class="mt-4 flex flex-wrap items-center justify-between gap-3 text-sm text-ink-muted">
            <span id="isp-search-summary"></span>
            <div class="flex items-center gap-4">
                <label class="flex items-center gap-2">
                    <span>Per page</span>
                    <select id="isp-page-size" class="field-input mt-0 w-auto py-1.5">
                        <option value="10">10</option>
                        <option value="20" selected>20</option>
                        <option value="50">50</option>
                    </select>
                </label>
                <div class="flex items-center gap-1">
                    <button type="button" id="isp-search-prev" class="icon-button" disabled aria-label="Previous page">
                        <svg viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.7" class="h-4 w-4"><path d="m12 4-6 6 6 6" stroke-linecap="round" stroke-linejoin="round" /></svg>
                    </button>
                    <span id="isp-page-number" class="min-w-[2rem] rounded-md bg-ink px-2.5 py-1 text-center text-xs font-semibold text-white">1</span>
                    <button type="button" id="isp-search-next" class="icon-button" disabled aria-label="Next page">
                        <svg viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.7" class="h-4 w-4"><path d="m8 4 6 6-6 6" stroke-linecap="round" stroke-linejoin="round" /></svg>
                    </button>
                </div>
            </div>
        </div>
    </section>
</div>

@section Scripts {
    <script type="module" src="~/js/pages/isp-admin.js"></script>
}

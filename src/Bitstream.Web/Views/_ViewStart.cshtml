@{
    // Shared with the Razor Pages side of the app (Pages/Shared/_Layout.cshtml) rather than a
    // second copy under Views/Shared — one navigation chrome, one header, for both.
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

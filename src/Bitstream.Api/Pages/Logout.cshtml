@page
@model Bitstream.Api.Pages.LogoutModel
@{
    ViewData["Title"] = "Signed out";
}

<section class="card mx-auto max-w-md" aria-labelledby="logout-heading">
    <h1 id="logout-heading" class="text-xl font-semibold">Sign out</h1>
    <p class="mt-2 text-sm text-ink-muted">Sign-out happens via the form in the header — there is nothing to see on this page directly.</p>
</section>

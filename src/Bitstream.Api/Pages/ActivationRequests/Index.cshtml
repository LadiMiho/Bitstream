@page
@model Bitstream.Api.Pages.ActivationRequests.IndexModel
@{
    ViewData["Title"] = "Activation Requests";
}

<section class="card" aria-labelledby="page-heading">
    <h1 id="page-heading" class="text-xl font-semibold">Activation Requests</h1>
    <p class="mt-2 max-w-2xl text-sm text-ink-muted">
        Submission, GIS verification and CRM-driven progress for activation requests
        (TRD §5) will be built here — this page is a placeholder reachable from the
        navigation until then.
    </p>
</section>
